import { FixMinimalRowForTablePipe } from './fix-minimal-row-for-table.pipe';

describe('FixMinimalRowForTablePipe', () => {
  it('create an instance', () => {
    const pipe = new FixMinimalRowForTablePipe();
    expect(pipe).toBeTruthy();
  });
});
